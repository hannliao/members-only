<div class="flex flex-col text-center items-center bg-white rounded-lg p-20">
  <% if (joined === true) { %>
  <h1 class="font-bold text-4xl m-5">Congrats!</h1>
  <p>You're a member of the secret club.</p>
  <% } else { %>
  <h1 class="font-bold text-4xl m-5">Sorry!</h1>
  <p>That's not the passcode.</p>
  <% } %>
  <a href="/account" class="underline p-4">Back to Dashboard</a>
</div>
