<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Sign Up</title>
    <link rel="stylesheet" href="/styles/tailwind.css" />
  </head>
  <body class="flex flex-col items-center justify-center bg-stone-100">
    <div class="mb-2">
      <% if (errors && errors.length) { %>
      <ul>
        <% errors.forEach((error) => { %>
        <li class="text-sm text-red-700"><%= error.msg %></li>
        <% }) %>
      </ul>
      <% } %>
    </div>

    <form
      action=""
      method="POST"
      class="w-full max-w-sm flex flex-col bg-white rounded-lg p-6 text-sm mb-1"
    >
      <h1 class="text-center text-lg mb-4"><%= title %></h1>
      <label for="firstname">First Name*</label>
      <input
        class="border rounded-full p-2 mb-2"
        id="firstname"
        name="firstname"
        type="text"
        maxlength="100"
        value="<%= typeof firstname !== 'undefined' ? firstname : '' %>"
        required
      />
      <label for="lastname">Last Name*</label>
      <input
        class="border rounded-full p-2 mb-2"
        id="lastname"
        name="lastname"
        type="text"
        maxlength="100"
        value="<%= typeof lastname !== 'undefined' ? lastname : '' %>"
        required
      />
      <label for="username">Username*</label>
      <input
        class="border rounded-full p-2 mb-2"
        id="username"
        name="username"
        type="text"
        maxlength="100"
        value="<%= typeof username !== 'undefined' ? username : '' %>"
        required
      />
      <label for="password">Password*</label>
      <input
        class="border rounded-full p-2 mb-2"
        id="password"
        name="password"
        type="password"
        maxlength="100"
        required
      />
      <label for="confirmPwd">Confirm Password*</label>
      <input
        class="border rounded-full p-2 mb-2"
        id="confirmPwd"
        name="confirmPwd"
        type="password"
        maxlength="100"
        required
      />
      <button
        class="bg-sky-700 rounded-full p-2 mt-5 text-white text-sm font-semibold"
      >
        Sign Up
      </button>
    </form>
    <p>
      Already have an account?
      <a href="/log-in" class="text-sky-700 underline">Log In</a>
    </p>
  </body>
</html>
